import React from 'react';
import styles from 'Sidebar.module.css'

function Sidebar(props) {
    return (
        <div styles={styles.sidebar}>
            navbar
        </div>
    );
}

export default Sidebar;