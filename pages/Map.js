import React from 'react';
import styles from '/styles/Map.module.css'

function Map(props) {
    return (
        <div styles={styles.map}>
            map
        </div>
    );
}

export default Map;